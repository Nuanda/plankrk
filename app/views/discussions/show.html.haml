- @discussion.comments.each do |comment|
  .box
    .avatar
      %img{ src: comment.author.image }
    .page-feed-content
      %small.time
        = comment.author.name
        = comment.created_at
      %p
        = comment.content
